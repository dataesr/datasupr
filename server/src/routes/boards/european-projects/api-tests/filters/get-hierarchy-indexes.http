### Test de création d'index pour get-hierarchy
GET http://localhost:3000/api/european-projects/get-hierarchy_indexes
Accept: application/json

> {%
client.test("Index creation executed successfully", function() {
  client.assert(response.status === 201, "Response status is not 201");
});

client.test("Response confirms index creation", function() {
  const contentType = response.contentType.mimeType;
  client.assert(contentType === "application/json", "Response is not JSON");
  
  const data = response.body;
  client.assert(data.message !== undefined, "Message field is missing");
  client.assert(data.indexName === "idx_hierarchy_covered", "Index name is incorrect");
  client.assert(data.description !== undefined, "Description field is missing");
});
%}
